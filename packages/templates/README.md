# Templates

`brainctl` embeds the v1 shared-brain seed and renders profile-specific install artifacts. This directory carries inspectable template notes and can grow into file-based templates when the embedded seed stabilizes.

Current rendered template groups:

- Shared-brain content repo seed.
- User systemd units.
- Git hooks.
- Tailscale enrollment and Serve scripts.
- Client bootstrap artifacts.

