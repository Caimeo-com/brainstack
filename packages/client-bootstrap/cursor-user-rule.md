# Shared Brain Rule

Before planning unfamiliar work, consult `__SHARED_BRAIN_LOCAL_PATH__` for decisions, machines, harnesses, skills, and runbooks. Sync with `git -C __SHARED_BRAIN_LOCAL_PATH__ pull --ff-only` when safe. For writes, use the shared-brain HTTP import/propose path or `brainctl`; do not directly mutate wiki pages unless explicitly instructed.
