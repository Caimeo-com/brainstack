schema_version: 1
profile: worker
machine:
  name: brain-worker
  user: operator
  role: worker
  sshUser: operator
  hostname: brain-worker
paths:
  home: /home/operator
  productRepo: ~/brainstack
  sharedBrainRoot: ~/shared-brain
  privateBrainRoot: ~/private-brain
  stateRoot: ~/.local/state/brainstack
  configRoot: ~/.config/brainstack
  systemdUserRoot: ~/.config/systemd/user
brain:
  bind: 127.0.0.1
  port: 8080
  publicBaseUrl: ""
  largeFileThresholdBytes: 10485760
telemux:
  enabled: false
tailscale:
  tailnetHost: brain-worker.example.ts.net
  controlTag: tag:brain
  workerTag: tag:brain-worker
  advertiseTags:
    - tag:brain-worker
  enableSsh: false

